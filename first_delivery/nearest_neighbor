NearestNeighbour(Node N)
    queue nodesSorted
    result = []
    Vt ← V
    
    while |Vt| > 0 do
        nodesSorted.resortRelativeTo(P)
        result.append(P)
        P ← result[0]
        nodesSorted.removeTop()
    
    return result